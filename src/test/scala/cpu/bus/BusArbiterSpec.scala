package cpu.bus

import chisel3._
import chiseltest._
import org.scalatest.freespec.AnyFreeSpec

class BusArbiterSpec extends AnyFreeSpec with ChiselScalatestTester {
  val portNum = 3
  "TODO" in {
    test(new BusArbiter(portNum)) { dut =>
      {}
    }
  }
}
